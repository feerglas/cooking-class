---
import { languages } from '../i18n/utils';
---
<ul>
  {Object.entries(languages)
    .map(([
      lang,
      label,
    ]) => (
      <li>
        <a href={`/${lang}/`}>{label}</a>
      </li>
    ))}
</ul>
